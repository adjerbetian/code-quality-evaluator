/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    singleQuote: true,
    printWidth: 120,
    tabWidth: 4,
    trailingComma: 'all',
    arrowParens: 'always',
};

export default config;
